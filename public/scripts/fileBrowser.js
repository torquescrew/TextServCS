// Generated by CoffeeScript 1.6.3
/*
Created by tobysuggate on 19/11/13.
*/

var currentEl, kAxis, kId, onDragging, onStartDrag, onStopDrag, setUpFileTree;

kId = "tree1";

kAxis = "horizontal";

currentEl = function() {
  return document.getElementById(kId);
};

onStartDrag = function(axis, id) {
  if (axis.length > 0) {
    kAxis = axis;
  }
  if (id.length > 0) {
    kId = id;
  }
  if (kAxis === "horizontal") {
    document.body.onmousemove = function(evt) {
      var w;
      w = evt.clientX;
      resizeTerm();
      return currentEl().setAttribute("style", "width:" + w + "px");
    };
  } else {
    document.body.onmousemove = function(evt) {
      var h;
      h = $(document).height() - (evt.clientY + 5);
      resizeTerm();
      return currentEl().setAttribute("style", "height:" + h + "px");
    };
  }
  return document.body.onmouseup = function() {
    return onStopDrag();
  };
};

onStopDrag = function() {
  resizeEditor();
  resizeTerm();
  document.body.onmousemove = null;
  return document.body.onmouseup = null;
};

onDragging = function() {
  if (kAxis === "horizontal") {
    return currentEl().setAttribute("style", "width:" + event.clientX + "px");
  } else {
    return currentEl().setAttribute("style", "height:" + ($(document).height() - (event.clientY + 5)) + "px");
  }
};

setUpFileTree = function() {
  var files, folders;
  folders = $(".folder");
  folders.mouseover(function() {
    return $(this).css("color", "#ffffff");
  });
  folders.mouseout(function() {
    return $(this).css("color", "#BBBBBB");
  });
  files = $(".file");
  files.mouseover(function() {
    return $(this).css("color", "#ffffff");
  });
  files.mouseout(function() {
    return $(this).css("color", "#999999");
  });
  $(".folder ul").hide();
  $(".folder").click(function(evt) {
    evt.stopPropagation();
    return $(this).children("ul").slideToggle(100);
  });
  $(".file").click(function(evt) {
    evt.stopPropagation();
    return openFile($(this).attr("id"));
  });
  return $(".myButton").hover((function() {
    $(this).css("color", "#ffffff");
    return $(this).css("background-color", "#111111");
  }), function() {
    $(this).css("color", "#bbbbbb");
    return $(this).css("background", "none");
  });
};
